[package]
script_dir = scripts
description = Enriches the DNS.log with fields from the input framework 
tags = RMF, HVA 

[input DNSenrichment.csv]
description = Enriching log by using the URI to add domain reputation to the dns.log
